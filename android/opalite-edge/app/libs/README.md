Place `librealsense.aar` here if the hosted Intel Maven dependency does not resolve.

Expected filename:
- `librealsense.aar`

Once present, `app/build.gradle` is already configured to use this local AAR instead of the remote dependency.
